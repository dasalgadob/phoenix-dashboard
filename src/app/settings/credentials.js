'use client'; // If used in Pages Router, is no need to add "use client"

import React from 'react';
import { Button } from 'antd';

const Credentials = () => (
  <div className="App">
    <Button type="primary">Credentials</Button>
  </div>
);

export default Credentials;