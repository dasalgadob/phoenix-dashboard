'use client'; // If used in Pages Router, is no need to add "use client"

import React from 'react';
import { Button } from 'antd';

const Alerts = () => (
  <div className="App">
    <Button type="primary">Alerts</Button>
  </div>
);

export default Alerts;